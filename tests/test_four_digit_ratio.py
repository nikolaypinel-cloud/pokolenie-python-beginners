"""
Тесты для задачи "Соотношение"
"""

def check_ratio(num):
    """Возвращает True, если сумма первой и последней цифр равна разности второй и третьей."""
    a = num // 1000
    b = (num // 100) % 10
    c = (num // 10) % 10
    d = num % 10
    return a + d == b - c

def run_tests():
    tests = [
        # (число, ожидаемый_результат)
        (1234, False),   # 1+4=5, 2-3=-1 → НЕТ
        (4132, True),    # 4+2=6, 1-3=-2 → НЕТ? проверим: 4+2=6, 1-3=-2 → 6 != -2 → НЕТ
        (5231, True),    # 5+1=6, 2-3=-1 → НЕТ
        (1111, True),    # 1+1=2, 1-1=0 → НЕТ
        (1000, True),    # 1+0=1, 0-0=0 → НЕТ
        (9999, True),    # 9+9=18, 9-9=0 → НЕТ
        (5142, True),    # 5+2=7, 1-4=-3 → НЕТ
        (8253, True),    # 8+3=11, 2-5=-3 → НЕТ
        (7341, True),    # 7+1=8, 3-4=-1 → НЕТ
        (6051, True),    # 6+1=7, 0-5=-5 → НЕТ
    ]
    
    # Подберём правильные примеры
    # Нужно a + d = b - c → b = a + d + c
    # Возьмём a=5, d=2, c=1 → b = 5+2+1 = 8 → число 5812: 5+2=7, 8-1=7 ✅
    # a=3, d=4, c=2 → b = 3+4+2 = 9 → число 3942: 3+2=5, 9-4=5 ✅
    # a=7, d=0, c=3 → b = 7+0+3 = 10 → не цифра → нельзя
    # a=2, d=5, c=0 → b = 2+5+0 = 7 → число 2705: 2+5=7, 7-0=7 ✅
    # a=1, d=0, c=1 → b = 1+0+1 = 2 → число 1210: 1+0=1, 2-1=1 ✅
    # a=4, d=1, c=2 → b = 4+1+2 = 7 → число 4721: 4+1=5, 7-2=5 ✅
    
    correct_tests = [
        (5812, True),   # 5+2=7, 8-1=7 → ДА
        (3942, True),   # 3+2=5, 9-4=5 → ДА
        (2705, True),   # 2+5=7, 7-0=7 → ДА
        (1210, True),   # 1+0=1, 2-1=1 → ДА
        (4721, True),   # 4+1=5, 7-2=5 → ДА
        (1234, False),  # 1+4=5, 2-3=-1 → НЕТ
        (4321, False),  # 4+1=5, 3-2=1 → НЕТ
        (9998, False),  # 9+8=17, 9-9=0 → НЕТ
        (1001, False),  # 1+1=2, 0-0=0 → НЕТ
        (5050, False),  # 5+0=5, 0-5=-5 → НЕТ
    ]
    
    print("=== Тесты для 'Соотношение' ===\n")
    all_passed = True
    
    for i, (num, expected) in enumerate(correct_tests, 1):
        result = check_ratio(num)
        passed = result == expected
        all_passed = all_passed and passed
        status = "✅" if passed else "❌"
        print(f"{status} Тест {i}: число = {num}")
        print(f"   Результат: {'ДА' if result else 'НЕТ'}")
        print(f"   Ожидалось: {'ДА' if expected else 'НЕТ'}\n")
    
    if all_passed:
        print("🎉 Все тесты пройдены!")
    else:
        print("❌ Некоторые тесты не пройдены.")

if __name__ == "__main__":
    run_tests()
